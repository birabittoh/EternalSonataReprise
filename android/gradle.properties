# Packaging the RelWithDebInfo native libs (~250 MB) OOMs the default heap.
org.gradle.jvmargs=-Xmx6g
# androidx.core FileProvider serves the self-update APK to the installer.
android.useAndroidX=true
